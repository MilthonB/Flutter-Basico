// Importaciones de terceros
import 'package:flutter/material.dart';

// importaciones propias
import 'package:contador/src/app.dart';

void main(){

    // Decirle a flutter que corra mi app
    // El runApp es metod que tiene material.dart y le estamos diciendo que la app la va a correr con el estilo de material
    runApp( const MyApp() );
}

